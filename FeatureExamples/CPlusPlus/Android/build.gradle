apply plugin: 'com.android.application'

// Relative path to engine source code directory. Modify as required.
def projectSourceDir = '..'
// Relative path to project source code directory. Modify as required.
def engineSourceDir = '../../../../../'
// Minimum required CMake version. This is same as in ../CMakeLists.txt.
def cmakeVersion = '3.22.1'

buildscript {
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:8.1.3'
        // NOTE: Do not place your application dependencies here; they belong
        // in the individual module build.gradle files
    }
}

android {
    namespace 'com.rengine.FeatureExamples'
    compileSdkVersion 33
    ndkVersion "26.3.11579264"
    defaultConfig {
        applicationId 'com.rengine'
        versionCode 1
        versionName '1.0.0'
        minSdkVersion 21
        targetSdkVersion 33
        ndk {
            abiFilters 'armeabi-v7a', 'arm64-v8a', 'x86_64'
        }
        externalNativeBuild {
            cmake {
                cppFlags '-std=c++20'
                arguments '-DANDROID_STL=c++_static', '-DANDROID_PLATFORM=android-26', '-DANDROID=1',
                        '-DATOMIC_SAMPLES=ON', '-DATOMIC_PLAYER=OFF',
                        '-DATOMIC_EDITOR=OFF', '-DATOMIC_PROFILING=OFF', '-DATOMIC_JAVASCRIPT=OFF',
                        '-DATOMIC_DOTNET=OFF'
            }
        }
    }
    externalNativeBuild {
        cmake {
            version "${cmakeVersion}"
            path "${engineSourceDir}/CMakeLists.txt"
            // due to path limit restrictions, we will put Android build outside engine path
            buildStagingDirectory "${engineSourceDir}../REngine-Android"
        }
    }
    buildTypes {
        release {
            minifyEnabled true
            proguardFiles getDefaultProguardFile('proguard-android.txt')

        }
    }
    sourceSets.main {
        manifest.srcFile 'AndroidManifest.xml'
        java.srcDirs = ['src', "${engineSourceDir}/Source/ThirdParty/SDL/android-project/app/src/main/java"]
        res.srcDirs = ['res']
        assets.srcDirs = ['assets']
    }
    compileOptions {
        sourceCompatibility JavaVersion.VERSION_1_8
        targetCompatibility JavaVersion.VERSION_1_8
    }
}

allprojects {
    repositories {
        google()
        mavenCentral()
    }
}